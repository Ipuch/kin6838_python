"""
KIN6838 - Cours 3

Matrice de rotation avec une séquence d'angles d'euler prédéfinies
"""
import sympy as sp

alpha = sp.symbols("alpha")
beta = sp.symbols("beta")
gamma = sp.symbols("gamma")

# remplir les matrices pour obtenir la matrice de rotation 
# avec une sequence de rotation
# XYZ

# Rotation matrix around X-axis
R_x = sp.Matrix([
    ...
])


# Rotation matrix around Y-axis
R_y = sp.Matrix([
    ...
])

# Rotation matrix around Z-axis
R_z = sp.Matrix([
    ...
])

R_xyz = 

# print(R_xyz)
# on affiche le resultat dans la console.
sp.pprint(R_xyz, use_unicode=True)